# AGENTS.md

`dml/` 保存可审计的初始化数据、参考数据或数据修复 SQL，不保存应用运行时查询。

- 脚本明确目标方言、前置 schema、执行顺序、适用环境和重复执行语义；seed 优先使用幂等键或冲突处理。
- 大批量更新、删除或回填要限制范围并说明分批、锁、失败恢复和回滚方案。
- 枚举、状态或内置数据变化时同步代码常量、schema、缓存和测试 fixture。
- 不提交生产导出、个人信息、真实账号、凭据或环境专属连接信息；只在隔离数据库或 fixture 中演练。
