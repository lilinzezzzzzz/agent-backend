# AGENTS.md

`examples/` 是可独立运行的演示代码，不是生产 entrypoint 或公共包 contract。

- 示例明确启动方式、外部服务、端口和凭据前置条件，网络服务默认绑定 loopback。
- API key 只从显式输入读取，不硬编码、回显或提交真实值。
- 邮件、支付、删除等外部副作用默认使用 mock；真实操作必须有清晰开关和用户确认。
- LLM / MCP tool 参数按不可信输入处理，保留结构化校验、未知工具处理、循环上限和错误边界。
- 生产代码不得依赖 `examples/`；真实外部调用的验证结论不得泛化为生产兼容性。
