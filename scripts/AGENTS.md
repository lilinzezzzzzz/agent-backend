# AGENTS.md

`scripts/` 保存仓库维护、验证和运维辅助脚本，不作为业务逻辑的唯一实现位置。

- 脚本校验命令、参数、操作系统和目标环境；删除、覆盖、重建、停止服务或操作 volume 前解析精确目标，
  拒绝 `/`、仓库根目录和默认生产路径。
- 临时资源使用唯一隔离目录，并只清理脚本自身创建的内容；失败返回非零退出码。
- 不读取或打印真实 secret。外部写入、生产操作和昂贵任务必须要求显式目标与确认。
- 脚本应可重复执行，保留足够且已脱敏的失败诊断。
- 按解释器运行语法检查；破坏性路径只在隔离资源上演练。
